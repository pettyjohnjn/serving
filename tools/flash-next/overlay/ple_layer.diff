--- a/vllm/models/qwen4_exp/nvidia/ple_layer.py
+++ b/vllm/models/qwen4_exp/nvidia/ple_layer.py
@@ -39,6 +39,10 @@
     Qwen4ExpTextConfig,
 )
 from vllm.utils.torch_utils import direct_register_custom_op
+from vllm.distributed import get_tensor_model_parallel_world_size, get_tp_group
+from vllm.logger import init_logger
+
+logger = init_logger(__name__)
 from vllm.v1.attention.backends.registry import MambaAttentionBackendEnum
 from vllm.v1.attention.backends.short_conv_attn import (
     PleShortConvAttentionBackend,
@@ -48,6 +52,34 @@
 from ..common.ple import PLEVocabParallelEmbedding
 from .ops.ple import ple_conv, ple_gate, ple_ngram_ids
 
+# --- Kai/2Wild 2026-09-05: disk-backed PLE table (see ops/ple_mmap.py) ---
+import os as _os
+from .ops.ple_mmap import PLEMmapTable
+
+def _ple_mmap_enabled() -> bool:
+    return _os.environ.get("QWEN4EXP_PLE_MMAP", "0") == "1"
+
+def _ple_resident_enabled() -> bool:
+    # Resident mode: same placeholder/shard-drop machinery as the disk mode, but
+    # each TP rank keeps ITS slice of the FP8 table as a plain GPU tensor behind
+    # the custom gather op. The table is not a Parameter, so torch.compile never
+    # sees it (no Inductor autotune copy, see vLLM PR #55272 by gau-nernst for
+    # that failure mode) and the fused compiled graph stays intact.
+    return _os.environ.get("QWEN4EXP_PLE_RESIDENT", "0") == "1"
+
+
+def _ple_staged_enabled() -> bool:
+    # Staged mode (for FULL CUDA graphs without torch.compile): the host-side row gather runs in the
+    # model state's prepare_inputs, BEFORE the graph replay, into a fixed GPU buffer that the captured
+    # graph reads. Shape of the idea: Trosfy's vLLM PR #54129 (gather in input prep + stable buffers);
+    # this is our own implementation on top of our preadv reader.
+    return _os.environ.get("QWEN4EXP_PLE_STAGED", "0") == "1"
+
+
+def _ple_mmap_threads() -> int | None:
+    v = _os.environ.get("QWEN4EXP_PLE_MMAP_THREADS")
+    return int(v) if v else None
+
 
 class Qwen4ExpPLEGroupedNorm(nn.Module):
     def __init__(
@@ -97,8 +129,14 @@
     ) -> None:
         del input_size, output_size, params_dtype
         weight_loader = extra_weight_attrs.get("weight_loader")
+        rows = sum(output_partition_sizes)
+        if _ple_mmap_enabled():
+            # Table stays on disk; keep a 1-row placeholder so the module
+            # graph, scale handling and dtype introspection stay intact.
+            rows = 1
+            layer._ple_mmap = True
         weight = create_fp8_weight_parameter(
-            sum(output_partition_sizes), input_size_per_partition, weight_loader
+            rows, input_size_per_partition, weight_loader
         )
         layer.register_parameter("weight", weight)
 
@@ -136,6 +174,16 @@
 ) -> QuantizeMethodBase | None:
     """Select global-scale FP8 only for quantized PLE checkpoint shards."""
 
+    if _ple_mmap_enabled():
+        # Disk-backed table requested: the FP8 embedding method is the one that
+        # knows how to keep a 1-row placeholder, and every checkpoint this patch
+        # is used with (NVIDIA ModelOpt, RadixArk, albucino's W4A16 + FP8 PLE)
+        # ships the table as FP8 shards plus one global weight_scale. Deciding
+        # from the checkpoint's quant config alone (below) misses the mixed
+        # builds, which then allocate the whole table slice in VRAM (2026-09-06,
+        # 4x3090 boot: 23.8 GiB per card, OOM before the weights).
+        return Qwen4ExpPLEFp8EmbeddingMethod()
+
     if isinstance(quant_config, ModelOptMixedPrecisionConfig):
         if quant_config._resolve_quant_algo(prefix) == "FP8":
             return Qwen4ExpPLEFp8EmbeddingMethod()
@@ -445,6 +493,14 @@
         query_start_loc: torch.Tensor,
         ngram_context: torch.Tensor,
     ) -> torch.Tensor:
+        staging = getattr(self, "_ple_staging", None)
+        if staging is not None:
+            # Staged mode: rows were gathered into `staging` by the model state before this
+            # forward (or before the graph replay). Only tensor views here: graph-safe.
+            # Layout: ngram_heads rows per token (row = head_dim FP8 values), token-major.
+            n = input_ids.reshape(-1).shape[0]
+            h = self.ngram_heads
+            return staging[: n * h].view(self._ple_dtype).view(n, h * self.head_dim)
         # Keep num_reqs-dependent ID generation outside PIECEWISE CUDA graphs,
         # which dispatch only on the padded token count.
         # torch.compile requires the splitting op to write graph-owned storage.
@@ -459,8 +515,129 @@
             ngram_ids,
             self.layer_name,
         )
+        if getattr(self.ngram_embedding, "_ple_mmap", False):
+            return self._ple_mmap_lookup(ngram_ids)
         return self.ngram_embedding(ngram_ids).flatten(-2)
 
+    # --- Kai/2Wild: staged gather (host side, before the graph) ------------
+    def _ple_init_staging(self, max_num_tokens: int, device: torch.device) -> None:
+        table = self._ple_mmap_table()
+        rows = int(max_num_tokens) * int(self.ngram_heads)
+        self._ple_staging = torch.zeros((rows, int(table.row_bytes)), dtype=torch.uint8, device=device)
+        logger.info(
+            "PLE staged gather: staging %d tokens x %d heads = %d rows x %d bytes on %s",
+            int(max_num_tokens), int(self.ngram_heads), rows, int(table.row_bytes), device,
+        )
+
+    def _ple_stage_zero(self, num_tokens: int) -> None:
+        self._ple_staging[: max(int(num_tokens), 0) * self.ngram_heads].zero_()
+
+    def _ple_stage_rows(
+        self,
+        input_ids: torch.Tensor,
+        query_start_loc: torch.Tensor,
+        ngram_context: torch.Tensor,
+        num_tokens: int,
+        padded_tokens: int,
+        real: bool = True,
+    ) -> None:
+        """Gather this step's rows into the staging buffer (called by the model state)."""
+        staging = self._ple_staging
+        h = self.ngram_heads
+        if not real or num_tokens <= 0:
+            self._ple_stage_zero(padded_tokens)
+            return
+        ids = self.compute_ngram_ids(input_ids[:num_tokens], query_start_loc, ngram_context)
+        table = self._ple_mmap_table()
+        rows = table.gather_cpu(ids.detach().reshape(-1).to("cpu", dtype=torch.int64))
+        staging[: num_tokens * h].copy_(rows, non_blocking=True)
+        if padded_tokens > num_tokens:
+            staging[num_tokens * h : padded_tokens * h].zero_()
+
+    # --- Kai/2Wild: disk-backed lookup path -------------------------------
+    def _ple_mmap_table(self) -> PLEMmapTable:
+        table = getattr(self, "_ple_table", None)
+        if table is None:
+            model_dir = getattr(self, "_ple_model_dir", None) or _os.environ.get(
+                "QWEN4EXP_PLE_MODEL_DIR"
+            )
+            if not model_dir:
+                raise RuntimeError(
+                    "PLE mmap: model dir unknown (set at PLE layer init or "
+                    "QWEN4EXP_PLE_MODEL_DIR)"
+                )
+            layer_idx = int(self.layer_name.rsplit("layers.", 1)[1].split(".", 1)[0])
+            table = PLEMmapTable(model_dir, layer_idx, threads=_ple_mmap_threads())
+            if self.ngram_embedding.org_vocab_size != table.rows_total:
+                raise ValueError(
+                    f"PLE mmap table rows {table.rows_total} != vocab "
+                    f"{self.ngram_embedding.org_vocab_size}"
+                )
+            self._ple_table = table
+            self._ple_row_bytes = int(table.row_bytes)
+            self._ple_dtype = table.dtype
+        return table
+
+    def _ple_mmap_lookup(self, ngram_ids: torch.Tensor) -> torch.Tensor:
+        # Traced by torch.compile: only tensor ops + constant attribute reads here.
+        # The table object is built eagerly in load_weights (thread pool + fds
+        # cannot be constructed under Dynamo tracing); the custom op body below
+        # is opaque to the tracer and does the host-side work.
+        row_bytes = self._ple_row_bytes
+        out = torch.empty(
+            (ngram_ids.numel(), row_bytes), dtype=torch.uint8, device=ngram_ids.device
+        )
+        torch.ops.vllm.qwen4_exp_ple_mmap_gather(ngram_ids, out, self.layer_name)
+        return out.view(self._ple_dtype).view(
+            ngram_ids.shape[0], self.ngram_heads * self.head_dim
+        )
+
+    def _ple_mmap_gather_into(self, ngram_ids: torch.Tensor, out: torch.Tensor) -> None:
+        resident = getattr(self, "_ple_resident", None)
+        if resident is not None:
+            # Rows owned by this rank come from the resident slice; rows owned by
+            # other ranks are written as zeros and filled by a byte-wise sum
+            # across TP (exactly one rank holds each row, so the sum is a copy).
+            start = self._ple_resident_start
+            local = ngram_ids.view(-1) - start
+            mask = (local >= 0) & (local < resident.shape[0])
+            idx = torch.where(mask, local, torch.zeros_like(local))
+            rows = torch.index_select(resident, 0, idx)
+            rows.mul_(mask.to(torch.uint8).unsqueeze(1))
+            out.copy_(rows)
+            if self._ple_tp_size > 1:
+                torch.distributed.all_reduce(out, group=get_tp_group().device_group)
+            return
+        table = self._ple_mmap_table()
+        rows = table.gather_cpu(ngram_ids.detach().to("cpu", dtype=torch.int64))
+        out.copy_(rows, non_blocking=True)
+
+    def _ple_load_resident_slice(self) -> None:
+        """Resident mode: pull this rank's row range into a GPU uint8 tensor."""
+        if getattr(self, "_ple_resident", None) is not None:
+            return  # load_weights can be visited twice; one slab per rank, never two
+        table = self._ple_mmap_table()
+        emb = self.ngram_embedding
+        start = int(emb.shard_indices.org_vocab_start_index)
+        end = int(emb.shard_indices.org_vocab_end_index)
+        count = max(0, min(end, table.rows_total) - start)
+        device = emb.weight.device
+        chunk = 1 << 20  # rows per host read (160 MiB at 160 B/row)
+        slab = torch.empty((count, table.row_bytes), dtype=torch.uint8, device=device)
+        done = 0
+        while done < count:
+            n = min(chunk * 8, count - done)
+            host = table.read_rows_contiguous(start + done, n)
+            slab[done : done + n].copy_(host, non_blocking=False)
+            done += n
+        self._ple_resident = slab
+        self._ple_resident_start = start
+        self._ple_tp_size = int(get_tensor_model_parallel_world_size())
+        logger.info(
+            "PLE resident slice: rows [%d, %d) = %.2f GiB on %s (tp=%d)",
+            start, start + count, count * table.row_bytes / 2**30, device, self._ple_tp_size,
+        )
+
     def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:
         """Load hash buffers and checkpoint-split embedding rows."""
 
@@ -508,6 +685,16 @@
                     min(shard_size, embedding.org_vocab_size - checkpoint_start),
                 )
                 expected_shape = (expected_rows, embedding.embedding_dim)
+                if getattr(embedding, "_ple_mmap", False):
+                    if tuple(loaded_weight.shape) != expected_shape:
+                        raise ValueError(
+                            f"Shape mismatch for PLE embedding shard {shard_index}: "
+                            f"expected {expected_shape}, got "
+                            f"{tuple(loaded_weight.shape)}"
+                        )
+                    del loaded_weight  # rows are served from the mmapped file
+                    loaded.add("ngram_embedding.weight")
+                    continue
                 if tuple(loaded_weight.shape) != expected_shape:
                     raise ValueError(
                         f"Shape mismatch for PLE embedding shard {shard_index}: "
@@ -525,6 +712,10 @@
 
         if regular_weights:
             loaded.update(AutoWeightsLoader(self).load_weights(regular_weights))
+        if getattr(self.ngram_embedding, "_ple_mmap", False):
+            self._ple_mmap_table()  # eager construction: fds + thread pool, outside any tracing
+            if _ple_resident_enabled():
+                self._ple_load_resident_slice()
         return loaded
 
 
@@ -567,6 +758,9 @@
             quant_config=quant_config,
             params_dtype=model_config.dtype,
         )
+        # Kai/2Wild: remember the checkpoint dir for the disk-backed PLE table
+        # (the forward runs outside the set_current_vllm_config() context).
+        self.ple_embedding._ple_model_dir = model_config.model
         # The PLE cache is TP-replicated, so this merged projection is too.
         self.kv_proj = MergedColumnParallelLinear(
             int(config.ple_embed_dim),
@@ -904,6 +1098,28 @@
     return
 
 
+
+# --- Kai/2Wild: disk-backed PLE gather as a splitting custom op -------------
+def _qwen4_exp_ple_mmap_gather(
+    ngram_ids: torch.Tensor, out: torch.Tensor, layer_name: str
+) -> None:
+    layer = get_forward_context().no_compile_layers[layer_name]
+    layer.ple_embedding._ple_mmap_gather_into(ngram_ids, out)
+
+
+def _qwen4_exp_ple_mmap_gather_fake(
+    ngram_ids: torch.Tensor, out: torch.Tensor, layer_name: str
+) -> None:
+    return None
+
+
+direct_register_custom_op(
+    op_name="qwen4_exp_ple_mmap_gather",
+    op_func=_qwen4_exp_ple_mmap_gather,
+    mutates_args=["out"],
+    fake_impl=_qwen4_exp_ple_mmap_gather_fake,
+)
+
 direct_register_custom_op(
     op_name="qwen4_exp_compute_ple_ngram_ids",
     op_func=qwen4_exp_compute_ple_ngram_ids,
