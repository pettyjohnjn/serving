# One API key per user. `serving keys add <name>` appends here; `serving keys revoke <name>`
# removes. Changes take effect on `serving restart`.
# Real file is mode 600 and git-ignored.
KEY_alice=sk-globus-alice-REPLACE_WITH_32_HEX_CHARS
